<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/black" /> <!-- Màu nền của viền -->
    <stroke
        android:width="2dp"
    android:color="@color/black" /> <!-- Màu sắc của viền -->
</shape>